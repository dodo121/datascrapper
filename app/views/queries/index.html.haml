- if user_signed_in?

  %h1 My queries

  - if current_user.queries.to_a.present?
    %table.table.table-bordered
      %tr
        %th Name
        %th{colspan:3} Options

      - current_user.queries.to_a.each do |query|
        %tr
          %td= query.name
          %td= link_to 'Show', query, class: 'btn btn-info btn-sm'
          %td= link_to 'Edit', edit_query_path(query), class: 'btn btn-default btn-sm'
          %td= link_to 'Destroy', query, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn btn-danger btn-sm'
  - else
    %br
    = link_to 'Add new Query &#10137;'.html_safe, new_query_path, class: 'btn btn-default'
- else
  %h1 Welcome!
  Features in this app:
  %ul
    %li Collecting google serach results based on query provided by user
    %li Checking specific link SEO position
    %li Checking if link goes up or down on serach results list
    %li Configurable interval-check time
