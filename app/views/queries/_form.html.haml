= simple_form_for(query) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name, required: :true

  .form-inputs
    = f.input :refresh_time, as: :integer, label: 'Refresh interval time (in minutes)'

  .form-actions
    = f.button :submit
